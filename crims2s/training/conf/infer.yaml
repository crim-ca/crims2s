defaults:
  - model: emos_normalnormal
  - optimizer: adam
  - transform: emos_gamma

hydra:
  job:
    name: S2S_Infer
  run:
    dir: ${oc.env:S2S_RUNS,.}/infer/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${oc.env:S2S_RUNS,.}/infer/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
user: ${oc.env:USER}
dataset_dir: /***HOME***fast_scratch/s2s/mlready/2021-09-08-w-features-test
num_workers: ${oc.env:SLURM_CPUS_PER_TASK,4}
begin: 2018
end: 2021
index:
logging:
  experiment_name: "S2S"
  mlflow_uri: "${oc.env:MLFLOW_TRACKING_URI,http://10.1.48.27:5000}"
  slurm_job_id: "${oc.env:SLURM_JOB_ID}"
checkpoint_dir: ???
output_file: ???