defaults:
  - dataset: eccc
  - module: bayes
  - model: bayes_multi
  - optimizer: spec_bayes_one_optim
  - transform: geography

dataset:
  include_features: True
early_stopping:
  patience: 10


batch_size: 4
trainer:
  accumulate_grad_batches: 1