defaults:
  - dataset: ncep
  - module: tercile
  - model: conv
  - optimizer: spec_adam
  - transform: full

include_features: True
early_stopping:
  patience: 10

batch_size: 4
backend:
  accumulate_grad_batches: 1