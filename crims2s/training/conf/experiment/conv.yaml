defaults:
  - dataset: ncep
  - module: tercile
  - model: conv
  - optimizer: adam
  - transform: geography

include_features: True
early_stopping:
  patience: 10

batch_size: 4

optimizer.weight_decay: 1e-6